/**
 * Arquivo mantido temporariamente apenas para preservar o caminho durante a limpeza da V2.
 * A V2 não usa SQLite nem banco em memória; testes ativos usam serviços puros ou contratos Supabase.
 */
export const LEGACY_SQLITE_TEST_DB_REMOVED = true;
